import ModalLayout from './ModalLayout';
import { ModalProps } from './ModalTech';

export default function ModalJob({ data, setData }: ModalProps) {
  return <ModalLayout title='직무'>ModalJob</ModalLayout>;
}
