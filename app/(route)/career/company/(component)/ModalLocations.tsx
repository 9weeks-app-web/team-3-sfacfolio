import ModalLayout from './ModalLayout';
import { ModalProps } from './ModalTech';

export default function ModalLocations({ data, setData }: ModalProps) {
  return <ModalLayout title='지역'>ModalLocations</ModalLayout>;
}
